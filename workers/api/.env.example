# Placeholder values only — safe to commit. NEVER put real secrets here.
#
# Real secrets go in:
#   - local dev:   workers/api/.dev.vars  (gitignored, NOT committed)
#   - production:  Cloudflare Worker secrets, set via:
#                    npx wrangler secret put USCIS_CLIENT_ID
#                    npx wrangler secret put USCIS_CLIENT_SECRET
#
# Never put secrets in wrangler.toml or any committed file.

USCIS_CLIENT_ID=replace_me
USCIS_CLIENT_SECRET=replace_me
